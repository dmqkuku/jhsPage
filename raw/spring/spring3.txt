Setter 메서드(수정자 메서드)를 통한 주입


일반 메서드를 통한 주입


생성자를 통한 주입


* XML에서 스프링은 Value에 지정한 텍스트 값을 적절한 자바 타입으로 변환해준다.

테스트 

"지저분한 코드를 정리한다고 조금 손을 댔는데, 이것 때문에 혹시 기존 기능에 문제가 생기진 않을까라는 불안한 마음으로 퇴근한 경험."

일단 해당 클래스에 main()메소드를 만들어서 직접 테스트를 구현할 수도 있다.

-> main()이 실행의 주도권을 지니고 있다.

반면. JUnit에서는 public @Test 메서드로 이를 대체할 수 있도록 지원한다.