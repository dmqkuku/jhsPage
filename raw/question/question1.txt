2022-03-24 
    MySql DB에 컬럼이 varchar(5);
    컬러명을 CD라고 한다면
    가져오는 문은
    SELECT IFNULL(MAX(CD)),0) + 1
			FROM 테이블
    Mybatis SelectKey이고 type은 String으로 들어가게 된다.

    기존에는 정수형 스트링으로 들어가다가. 어느날 부터 실수형으로 들어가기 시작.

    https://mariadb.com/kb/en/addition-operator/
    + operator.
    For real or string operands, the operand with the highest precision determines the result precision.